# Base URL of the Go backend API. In local dev the Vite dev server also proxies
# /api to the backend (see vite.config.ts), so the default works out of the box.
VITE_API_URL=http://localhost:8000/api
